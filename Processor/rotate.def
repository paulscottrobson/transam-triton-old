//*******************************************************************************************************
//*******************************************************************************************************
//
//							 				Rotate and Shift Groups
//
//*******************************************************************************************************
//*******************************************************************************************************

07			4 	"RLCA"			carry = (a >> 7) & 1;a = (a << 1) | carry;negate = halfCarry = 0;
17 			4 	"RLA"			temp16 = (a << 1) | carry;carry = (temp16 >> 8);a = temp16 & 0xFF;negate = halfCarry = 0;
0F 			4	"RRCA"			carry = a & 1;a = (a >> 1) | (carry << 7);negate = halfCarry = 0;
1F 			4 	"RRA"			temp16 = a | (((WORD16)carry) << 8);carry = temp16 & 1;a = (temp16 >> 1) & 0xFF

